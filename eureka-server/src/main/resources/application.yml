server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false   # Eureka ne s'enregistre pas lui-mme
    fetch-registry: false          # Il ne cherche pas d'autres Eureka
  server:
    wait-time-in-ms-when-sync-empty: 0